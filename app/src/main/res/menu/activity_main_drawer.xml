<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    tools:keep="@menu/activity_main_drawer">
    <item android:checkableBehavior="single"
        android:title="@string/cadastros"
        android:visible="true">

        <menu>
            <item
                android:id="@+id/nav_avaliador"
                android:icon="@android:drawable/ic_menu_my_calendar"
                android:title="@string/avaliador"
                android:visible="true" />

            <item android:id="@+id/nav_medidores"
                android:icon="@android:drawable/ic_menu_set_as"
                android:title="@string/medidores"
                android:visible="true" />

        </menu>
    </item>
    <item android:title="@string/testes"
        android:visible="true">

        <menu>
            <item android:id="@+id/nav_relatorioVerificação"
                android:icon="@android:drawable/ic_menu_preferences"
                android:title="@string/relat_rio_de_verifica_o"
                android:visible="true" />
        </menu>

    </item>
</menu>
